import AddProjects from "@/components/Admin/Projects/AddProjects";
import React from "react";

const page = () => {
  return <>{/* <AddProjects projectData={projectData} /> */}</>;
};

export default page;
